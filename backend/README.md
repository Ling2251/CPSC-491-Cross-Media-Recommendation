# Backend

Not started yet. This directory will hold the API that serves media, tags,
and recommendations to the `frontend/` app and reads/writes the schema in
`database/`.

Planned for a later sprint.
